/** Host loader entry for the optional Developer Workbench browser plugin. */

/** Provides no host-side behavior; the browser half owns presentation registration. */
export function apply(): void {}
